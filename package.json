{
  "name": "capy-onsen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun ./index.html",
    "build": "bun build ./index.html --outdir dist --minify",
    "test": "bun test --path-ignore-patterns test/visual/**",
    "test:visual": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
